var EC = artifacts.require("./ECTools.sol")
var LC = artifacts.require("./LedgerChannel.sol")

module.exports = async function(deployer) {
  // let ec = await EC.new()
  // LC.link('ECTools', ec.address)
  // lc = await LC.new()
  // console.log('ledger channel address: ' + lc.address)

}